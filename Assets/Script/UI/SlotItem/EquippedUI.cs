using System;
using System.Collections;
using System.Collections.Generic;
using Google.Protobuf.Enum;
using UnityEngine;

public class EquippedUI : BaseUI
{
    //enum GameObjects
    //{
    //    WeaponEquip,
    //    ShieldEquip,
    //    HelmetEquip,
    //    ArmorEquip,
    //    BootsEquip,
    //    GlovesEquip,
    //    PendantEquip,
    //    RingEquip,
    //    Ring2Equip
    //}

    //GameObject _weaponSlot;
    //GameObject _shieldSlot;
    //GameObject _helmetSlot;
    //GameObject _armorSlot;
    //GameObject _bootsSlot;
    //GameObject _glovesSlot;
    //GameObject _pendentSlot;
    //GameObject _firstRingSlot;
    //GameObject _secondRingSlot;

    

    //protected override void Awake()
    //{
    //    base.Awake();
    //    Bind<GameObject>(typeof(GameObjects));

    //    _weaponSlot = Get<GameObject>((int)GameObjects.WeaponEquip);
    //    _shieldSlot = Get<GameObject>((int)GameObjects.ShieldEquip);
    //    _helmetSlot = Get<GameObject>((int)GameObjects.HelmetEquip);
    //    _armorSlot = Get<GameObject>((int)GameObjects.ArmorEquip);
    //    _bootsSlot = Get<GameObject>((int)GameObjects.BootsEquip);
    //    _glovesSlot = Get<GameObject>((int)GameObjects.GlovesEquip);
    //    _pendentSlot = Get<GameObject>((int)GameObjects.PendantEquip);
    //    _firstRingSlot = Get<GameObject>((int)GameObjects.RingEquip);
    //    _secondRingSlot = Get<GameObject>((int)GameObjects.Ring2Equip);
    //}

    //public void Refresh(List<Equipment> equipItems)
    //{
    //    foreach (Equipment equip in equipItems)
    //    {
    //        switch (equip.SlotType)
    //        {
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //            case ESlotType.Weapon:
    //                Utils.GetOrAddComponent<EquippedSlot>(_weaponSlot).SetInfo(equip);
    //                break;
    //        }
    //    }
    //}
}
